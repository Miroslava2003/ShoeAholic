@{
    ViewData["Title"] = "Съобщението е изпратено";
}

<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-lg-6 text-center">
            <div class="card shadow-sm border-0">
                <div class="card-body p-5">
                    <div class="mb-4"><i class="bi bi-check-circle text-success" style="font-size: 5rem;"></i></div>
                    <h2 class="mb-3">Благодарим Ви!</h2>
                    <p class="text-muted mb-4">Вашето съобщение беше изпратено успешно. Ще се свържем с Вас възможно най-скоро.</p>
                    @if (TempData["Success"] != null)
                    {
                        <div class="alert alert-success"><i class="bi bi-info-circle"></i> @TempData["Success"]</div>
                    }
                    <div class="d-grid gap-2 mt-4">
                        <a asp-controller="Home" asp-action="Index" class="btn btn-primary btn-lg"><i class="bi bi-house"></i> Към началната страница</a>
                        <a asp-controller="Products" asp-action="Index" class="btn btn-outline-secondary"><i class="bi bi-shop"></i> Разгледай продуктите</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .btn-primary {
        background-color: #9528de;
        border-color: #9528de;
    }

        .btn-primary:hover {
            background-color: #7a1fb8;
            border-color: #7a1fb8;
        }

    .text-success {
        color: #28a745 !important;
    }
</style>
